{
  "name": "pr-noise-detector-action",
  "version": "1.0.0",
  "description": "GitHub Action to detect superfluous files in pull requests",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}